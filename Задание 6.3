/*
 ============================================================================
 Name        : 3.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(void) {
 int N, A, B;
 scanf("%d %d %d", &N, &A, &B);
 int m[N];
 m[N] = (int*)malloc(N*sizeof(int));
 for(int i = 0; i<N; i++){
  m[i] = A + rand()%(B-A+1);
 }
 printf("Массив: ");
 for(int i = 0; i<N; i++){
  printf("%d ", m[i]);
 }

 int k = 0;
 int number1 = 0;
 int number2 = 0;
 //printf("\nНайденные числа: ");
 //for(int i = 0; i<N && k<2;i++){
 for(int i = 0; i<N; i++){
	  if(m[i] > 0 && m[i]%5==0){
		   printf("%d ", m[i]);
		   k++;
		   if (k==1){
		   	  number1 = m[i];
		   	}
		   	else if(k==2){
		   		  number2 = m[i];
		   	}
	  	  }
	   if(k==2){
		   break;
		}
  }

 if (k>2 || k == 2){
	 printf("\nНайденные числа: ");
 	  printf("%d %d", number1, number2);
   }
   else{
 	  printf("\n%s", "заданных чисел не обнаружено");
   }

 return EXIT_SUCCESS;
}
